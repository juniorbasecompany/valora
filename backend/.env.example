# Copie para .env e ajuste conforme o ambiente.
# O .env não deve ser commitado.

# URL do PostgreSQL (padrão compatível com docker-compose local)
# DATABASE_URL=postgresql+psycopg://valora:dev@localhost:5432/valora
