# Copie o ficheiro `.env.example` da raiz do monorepo para `.env` na raiz e defina `POSTGRES_PASSWORD`.
# O backend lê `../.env` (e opcionalmente `.env` nesta pasta) para a mesma variável.
# Não commite ficheiros `.env`.
